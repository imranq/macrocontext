<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @series.title %>
</p>

<p>
  <strong>Series:</strong>
  <%= @series.series_id %>
</p>
<div class="sparkchart"><%= line_chart SeriesDatum.where(:series_id => @series.series_id, :date => 10.year.ago..Time.now).group_by_year(:date).average(:value) %></div>

<p>
  <strong>Source:</strong>
  <%= @series.description #use redactor/ckeditor %>
</p>

<p>
  <strong>Source:</strong>
  <%= @series.source %>
</p>

<%= link_to 'Edit', edit_series_path(@series) %> |
<%= link_to 'Back', series_index_path %>
